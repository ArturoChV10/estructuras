#ifndef _COLACARGA_
#define COLACARGA 0

#include "lista.h"

class carga {
private:
//    metodos colaCarga;

public:
    carga() {
        //Constructor para definir parámetros
    }

    bool isEmpty() {
//        return colaCarga.isEmpty();
    }

    void enqueue(void *pElement) {
//        colaCarga.addToEnd(pElement);
    }

    void *dequeue() {
//        return colaCarga.removeFirst();
    }
};

#endif