#ifndef _QUEUE_
#define _QUEUE_ 0

#include "lista.h"

struct queue {
    struct metodos cola;

    //"la cola de carga para subir al camion podríamos pensar en ordenes de cosas que hay que subir a un camion"

    bool isEmpty() {
        //Métodos aplicados sobre la cola
    };

    void push(void* pElement) {
        //Métodos aplicados sobre la cola
    };

    void* pop() {
        //Métodos aplicados sobre la cola
    }

};

#endif